# Context Snapshots

> Written by `/context-save`, read by `/context-restore` (commands/19).
> One file per snapshot: `YYYY-MM-DD.md`. Never edit old snapshots —
> they are the audit trail of deal state.
