# Interactive Seed Deck (Slidev)

A code-built deck — proof in itself that the founders are technical.

```bash
cd deck
npm install
npm run dev      # live at localhost:3030 — presenter mode included
npm run export   # PDF for sending (investor-package rule: send PDF only)
```
Replace every ⟦DATA⟧ from company-layer/ + context/ before any external use.
The LIVE PROOF slide embeds a real demo — rehearse it; keep a recorded backup.
